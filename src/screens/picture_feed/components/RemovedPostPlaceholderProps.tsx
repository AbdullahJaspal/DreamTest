import React from 'react';

interface RemovedPostPlaceholderProps {
  postId: number;
}

const RemovedPostPlaceholder: React.FC<RemovedPostPlaceholderProps> = () => {
  return null;
};

export default React.memo(RemovedPostPlaceholder);
