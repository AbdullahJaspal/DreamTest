buildscript {
    ext {
        buildToolsVersion = "35.0.0"
        minSdkVersion = 24
        compileSdkVersion = 35
        targetSdkVersion = 34
        ndkVersion = "26.1.10909125"
        kotlinVersion = "1.9.22"
        ffmpegKitPackage = "full-gpl"
        googlePlayServicesAuthVersion = "20.7.0"
        supportLibVersion = "28.0.0" 
        androidXAnnotation = "1.1.0"
        androidXBrowser = "1.0.0"
        useExoplayerHls = true
    }
    // repositories {
    //     google()
    //     mavenCentral()
    // }
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal() // Add this line
    }
    // dependencies {
    //     classpath("com.android.tools.build:gradle")
    //     classpath("com.facebook.react:react-native-gradle-plugin")
    //     classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
    //     classpath('com.google.gms:google-services:4.4.2')
    // }
    dependencies {
        classpath("com.android.tools.build:gradle:8.2.2") // Use a specific version
        classpath("com.facebook.react:react-native-gradle-plugin") // Keep this line
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.21") // Use a specific version
        classpath("com.google.gms:google-services:4.4.1") // Use the latest version
    }
} 

apply plugin: "com.facebook.react.rootproject"
